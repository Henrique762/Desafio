apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-b-dev
  namespace: dev 
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-b-dev
##### Config de Template do Pod ####
  template:
    metadata:
      labels:
        app: app-b-dev 
#### Config de Specs do Container #####
    spec:
      containers:
        - name: app-b-dev
          image: henrique77/dev-appb
          ports:
          - containerPort: 80
            name: nginx
            protocol: TCP
