apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-d-dev
  namespace: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-d-dev
##### Config de Template do Pod ####
  template:
    metadata:
      labels:
        app: app-d-dev
#### Config de Specs do Container #####
    spec:
      containers:
        - name: app-d
          image: henrique77/dev-appd
          ports:
          - containerPort: 80
            name: nginx
            protocol: TCP
